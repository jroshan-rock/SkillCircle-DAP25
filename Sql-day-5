#2026 -03 - 16 
-- from => join => where => group by => having => select => order by => limit => offset

SQL operators are reserved words or characters used in SQL statements to perform operations like comparisons, calculations, 
and logical evaluations on data. 

They are crucial for filtering, manipulating, and retrieving specific data sets from a database.
# Operator:- 
1. Arithmetical Operator ( + , - , * , / , % ) 
2. Assignment Operator ( =, +=, -= , *= , /=, %=) 
3. Comparision Operator ( < , > , != , <> , <= , >= ) 
4. Logical Operator ( OR, AND , XOR , NOT ) 
5. Special Operator 
SQL provides several special operators used primarily in WHERE clauses for specific, 
complex data filtering operations that go beyond basic comparisons. 
Key special operators include IN, BETWEEN, LIKE, IS NULL, and EXISTS. 
- Between , in , not in , is , is not , like 

select * from students;
select * from dataset;
select * from customer_segmentation_data;


1. Arithmetical Operator ( + , - , * , / , % ) 
select * from dataset;

-- Calculate the 5 % of medical charges ? 
-- Calculate the 5 % of medical charges for Gender  is male ? 

select `medical charges` , `medical charges` * (5/100) as medical_charges 
from dataset;

select `medical charges` , `medical charges` * 0.05 as medical_charges 
from dataset;

select `medical charges` , `medical charges` * 0.05 as medical_charges 
from dataset where gender = 'male';

-- Find the 2% and 5 % BMI for Gender is Female ?  
-- Find the 2% and 5 % BMI for Smoker is Yes ?  
select * ,(bmi + bmi * 0.05) as 5_per_inc_bmi , 
(bmi + bmi * 0.02) as 2_per_inc_bmi  
from datase;

select * ,(bmi + bmi * 0.05) as 5_per_inc_bmi , 
(bmi + bmi * 0.02) as 2_per_inc_bmi  
from dataset where gender = 'male';

select * ,(bmi + bmi * 0.05) as 5_per_inc_bmi , 
(bmi + bmi * 0.02) as 2_per_inc_bmi  
from dataset where smoker ='yes';


2. Comparision Operator ( < , > , != , <> , <= , >= ) 
-- Fetch the all medical reports for age is more than 25 ? 
select * from dataset where age >25;
select count(*) from dataset where age >25;

-- Fetch the all medical reports for region is not southeast?
select * from dataset where region!='southeast';
select count(*) from dataset where region<>'southeast';

-- Show the children , smoker , gender , age columns for the children is 0 ? 
select children , smoker , gender , age from dataset where children = 0;

-- Show the all patients reports for bmi  less than or equal 25.0 ? 
select * from dataset where bmi <='25.0';
select count(*) from dataset where bmi <='25.0'

3. Logical and , or , not 

-- AND ( both condition should be true) 
TRUE and FALSE -> FALSE 
FALSE and FALSE -> FALSE 
TRUE and TRUE -> TRUE 
False and True -> False

-- OR  ( Atleast any one conditions should be true ) 
True or False -> True 
False or False -> False 
False or True -> True 
True or True -> True 

-- XOR ( Should be only one conditons true )
True or False -> True 
False or False -> False 
False or True -> True 
True or True -> False

-- NOT 
True -> False 
false -> True

select * from dataset;

-- Fetch the all medical reports for gender is female and smoker is yes ? 
select * from dataset where gender = 'female' and smoker ='yes';
select count(*) from dataset where gender = 'female' and smoker ='yes';

-- Fetch the all medical reports for gender is male and smoker is yes ? 
select * from dataset where gender = 'male' and smoker ='yes';
select count(*) from dataset where gender = 'male' and smoker ='yes';

-- in , not in, between , is , is not , 

-- Between and in 
1. The SQL Between and in operator is a logical operator used in the where clause to specific multiple values for a column. 
It allows you to filter based on whether a columns' values mathces any within a specificed list or the result of a subquery.

1. Between is a logical opertor used for filter record based on specific range . 
Description: Checks if a value falls within a specified range, inclusive of the start and end values. 
It can be used with numbers, dates, and characters.

-- Age should be more than 18 and less than 35 for smoker is yes;
select * from dataset where age > 18 and age <35 and smoker= 'yes'
order by `medical charges` desc;

-- Age should be more than 18 or less than 35 for smoker is yes;
select * from dataset where (age > 18 or age <35) and smoker= 'yes'
order by `medical charges` desc;

-- Range  ( between ) 
select * from dataset where (age >= 18 and age <=35) and smoker= 'yes'
order by `medical charges` desc;

select * from dataset where age between 18 and 35 and smoker= 'yes'
order by `medical charges` desc;

-- Between  ( use for filtring range value like age , date/time etc ) 
SELECT COUNT(*) FROM medical_costs 
WHERE Age BETWEEN 18 AND 25;

👉 BETWEEN is inclusive — it includes both the boundary values.
So this query counts all rows where
Age >= 18 AND Age <= 25.

SELECT COUNT(*) FROM medical_costs 
WHERE Age > 18 AND Age < 25;

👉 This query is exclusive — it does not include 18 and 25.
It only counts rows where
Age is strictly greater than 18 and strictly less than 25.

select * from medical_costs where Age between 25 and 30;


2. -- For multiple  or condition ( in or not in ) Check for multiple or Condition on same colums
Description: Determines if a value matches any value in a provided list of expressions or the result of a subquery. 
It simplifies queries that would otherwise require multiple OR conditions.

-- Age is 18, 21,25,30 
select * from dataset where age = 18 or age = 21 or age = 25 or age = 30 ;
select * from dataset where age in(18,21,25,30);

-- Not for southeast , southwest 
select * from dataset where region not in('southeast','southwest');


-- Children is 2 or 5 ? 
select * from medical_costs where children in(2,5);

-- Children is not 2 or 5 ?
select * from medical_costs where children not in(0,1);

3. -- IS NULL / IS NOT NULL 
-- Only used for check the null value in a specific column  or set of columns

Description: Tests for NULL values (missing or unknown data) in a column, as standard comparison operators (=, <>) don't work reliably with NULL.
Example: SELECT * FROM Orders WHERE Shipping_Date IS NULL;

-- Fetch the medical reports where BMI is null or not null
select * from medical_costs
where BMI is null;

select * from medical_costs where BMI is not null;

-- fetch the all medical reports where smoker is not null
select * from medical_costs 
where Smoker is not null;

4. Like , not Like 
Description: Searches for a specific pattern within a string column using wildcard characters. 
The commonly used wildcards are % (zero or more characters) and _ (a single character).
Example: SELECT * FROM Customers WHERE Email LIKE '%@gmail.com';



